import VButton from './button/Button';
import VSearch from './search/Search';
import VTable from './table/Table';

export default {
    name: 'baseComponent',
    components: {
        VButton,
        VSearch,
        VTable
    }    
}
